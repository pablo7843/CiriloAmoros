---
import BaseLayout from "../layouts/BaseLayout.astro";
import CtaFinal   from "../components/CtaFinal.astro";
import Faq        from "../components/Faq.astro";
import { services } from "../data/services";
import { site } from "../data/site";

const rows = [
  { label: "Precio base",             despacho: `desde ${services[0].priceFrom} €/mes`, sala: `${services[1].priceFrom} €/hora`, virtual: `desde ${services[2].priceFrom} €/mes` },
  { label: "Nota de precio",          despacho: "Sin IVA", sala: "Sin IVA", virtual: "Sin IVA" },
  { label: "Acceso 24/7",             despacho: true,  sala: false, virtual: false },
  { label: "Llave / acceso propio",   despacho: true,  sala: false, virtual: false },
  { label: "Recepción de visitas",    despacho: true,  sala: true,  virtual: true  },
  { label: "Dirección comercial",     despacho: true,  sala: false, virtual: true  },
  { label: "Dirección fiscal",        despacho: true,  sala: false, virtual: true  },
  { label: "Recepción de correo",     despacho: true,  sala: false, virtual: true  },
  { label: "Internet incluido",       despacho: true,  sala: true,  virtual: false },
  { label: "Limpieza",                despacho: true,  sala: true,  virtual: false },
  { label: "Climatización",           despacho: true,  sala: true,  virtual: false },
  { label: "Uso de sala de reuniones",despacho: "Incluido puntual", sala: "Por horas", virtual: "Precio especial" },
  { label: "Permanencia mínima",      despacho: "[PENDIENTE]", sala: "Sin contrato", virtual: "[PENDIENTE]" },
];
---

<BaseLayout
  title="Tarifas · Precios de despachos, salas y domiciliación en Valencia"
  description="Precios transparentes sin sorpresas. Despachos desde 320 €/mes, salas desde 16 €/hora, oficina virtual desde 40 €/mes. Todos sin IVA. Cirilo Amorós Centro Empresarial, Valencia."
>

  <!-- ── Cabecera ── -->
  <section class="border-b border-gray-100 bg-white py-16 sm:py-20">
    <div class="mx-auto max-w-7xl px-6">

      <nav aria-label="Ruta de navegación" class="mb-8">
        <ol class="flex items-center gap-2 text-xs text-gray-400">
          <li><a href="/" class="transition-colors hover:text-gray-600">Inicio</a></li>
          <li aria-hidden="true">›</li>
          <li class="font-medium text-gray-700" aria-current="page">Tarifas</li>
        </ol>
      </nav>

      <div class="max-w-2xl">
        <p class="hero-enter mb-3 text-[10px] font-bold uppercase tracking-[0.2em] text-accent-600" style="animation-delay: 0ms">Tarifas</p>
        <h1 class="hero-enter text-[2.5rem] font-bold leading-[1.08] tracking-tight text-gray-900 sm:text-5xl" style="animation-delay: 100ms">
          Precios transparentes.<br />Sin sorpresas.
        </h1>
        <p class="hero-enter mt-5 text-base leading-relaxed text-gray-500" style="animation-delay: 240ms">
          Todos los precios son sin IVA (21%). Una cuota fija que cubre todo lo que necesita.
          Sin costes de comunidad, sin suministros aparte, sin mantenimiento.
        </p>
      </div>
    </div>
  </section>

  <!-- ── Cards de precios destacados ── -->
  <section aria-label="Precios principales" class="bg-gray-50 py-14 sm:py-16">
    <div class="mx-auto max-w-7xl px-6">
      <div class="grid grid-cols-1 gap-5 sm:grid-cols-3">

        {services.map((s, i) => (
          <a
            href={s.slug}
            data-reveal
            style={`transition-delay: ${i * 100}ms`}
            class={[
              "group flex flex-col rounded-2xl p-7 transition-shadow hover:shadow-lg",
              i === 0
                ? "bg-gray-950 text-white"
                : "bg-white text-gray-900 ring-1 ring-gray-200",
            ].join(" ")}
          >
            <p class={`mb-4 text-[10px] font-bold uppercase tracking-[0.2em] ${i === 0 ? "text-gray-500" : "text-accent-600"}`}>
              {s.name}
            </p>
            <div class="flex items-baseline gap-1">
              <span class="text-4xl font-bold tracking-tight">{s.priceFrom} €</span>
              <span class={`text-sm ${i === 0 ? "text-gray-400" : "text-gray-500"}`}>/{s.priceUnit}</span>
            </div>
            <p class={`mt-1 text-xs ${i === 0 ? "text-gray-500" : "text-gray-400"}`}>{s.priceNote}</p>
            <p class={`mt-4 flex-1 text-sm leading-relaxed ${i === 0 ? "text-gray-400" : "text-gray-600"}`}>
              {s.description}
            </p>
            <div class={`mt-6 flex items-center gap-1 text-sm font-semibold ${i === 0 ? "text-white" : "text-accent-600"} transition-gap group-hover:gap-2`}>
              Ver detalles
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="m9 18 6-6-6-6"/>
              </svg>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ── Tabla comparativa ── -->
  <section aria-labelledby="tabla-titulo" class="bg-white py-20 sm:py-24">
    <div class="mx-auto max-w-5xl px-6">

      <div class="mb-12" data-reveal>
        <p class="mb-3 text-[10px] font-bold uppercase tracking-[0.2em] text-accent-600">Comparativa</p>
        <h2 id="tabla-titulo" class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          ¿Cuál es el plan para usted?
        </h2>
      </div>

      <div class="overflow-x-auto" data-reveal style="transition-delay: 120ms">
        <table class="min-w-full">
          <thead>
            <tr class="border-b border-gray-200">
              <th scope="col" class="py-4 pr-4 text-left text-sm font-medium text-gray-500 w-[40%]">
                Característica
              </th>
              {services.map((s, i) => (
                <th
                  scope="col"
                  class={`px-4 py-4 text-center text-sm font-bold ${i === 0 ? "text-gray-900" : "text-gray-700"}`}
                >
                  {s.name}
                  <span class={`block text-xs font-normal mt-0.5 ${i === 0 ? "text-accent-600" : "text-gray-400"}`}>
                    desde {s.priceFrom} €/{s.priceUnit}
                  </span>
                </th>
              ))}
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            {rows.slice(2).map((row) => (
              <tr class="hover:bg-gray-50 transition-colors">
                <td class="py-4 pr-4 text-sm text-gray-700">{row.label}</td>
                {[row.despacho, row.sala, row.virtual].map((val) => (
                  <td class="px-4 py-4 text-center text-sm">
                    {val === true ? (
                      <span class="inline-flex justify-center">
                        <svg class="h-5 w-5 text-accent-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-label="Incluido">
                          <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"/>
                        </svg>
                      </span>
                    ) : val === false ? (
                      <span class="inline-flex justify-center">
                        <svg class="h-5 w-5 text-gray-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-label="No incluido">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12"/>
                        </svg>
                      </span>
                    ) : (val as string).startsWith("[PENDIENTE") ? (
                      <span class="text-xs italic text-gray-300">Consultar</span>
                    ) : (
                      <span class="text-gray-600">{val}</span>
                    )}
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <!-- Notas -->
      <div class="mt-8 space-y-2 border-t border-gray-100 pt-6">
        <p class="text-xs text-gray-400">
          * Todos los precios son sin IVA. El IVA aplicable es el 21%.
        </p>
        <p class="text-xs text-gray-400">
          * Los precios son orientativos y pueden variar según el espacio elegido. Contáctenos para un presupuesto personalizado.
        </p>
        <p class="text-xs text-gray-400">
          * [PENDIENTE: confirmar permanencia mínima y precios exactos de planes de oficina virtual con el centro]
        </p>
      </div>
    </div>
  </section>

  <!-- ── ¿Tiene dudas? ── -->
  <section class="bg-gray-50 py-16 sm:py-20">
    <div class="mx-auto max-w-4xl px-6 text-center" data-reveal>
      <h2 class="text-2xl font-bold text-gray-900">¿No sabe cuál es su plan ideal?</h2>
      <p class="mt-3 text-base text-gray-500">
        Cuéntenos su situación y le asesoramos sin compromiso. Hay un plan para cada caso.
      </p>
      <div class="mt-8 flex flex-wrap items-center justify-center gap-4">
        <a
          href="/contacto"
          class="inline-flex items-center rounded-xl bg-accent-600 px-6 py-3.5 text-sm font-semibold text-white shadow-sm transition-colors hover:bg-accent-700 focus:outline-none focus:ring-2 focus:ring-accent-500 focus:ring-offset-2"
        >
          Solicitar asesoramiento
        </a>
        <a
          href={site.contact.phoneHref}
          class="inline-flex items-center gap-2 text-sm font-medium text-gray-600 transition-colors hover:text-gray-900"
        >
          <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 0 0 2.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 0 1-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 0 0-1.091-.852H4.5A2.25 2.25 0 0 0 2.25 4.5v2.25Z"/>
          </svg>
          {site.contact.phone}
        </a>
      </div>
    </div>
  </section>

  <!-- ── FAQ de precios ── -->
  <Faq category="precios" />

  <!-- ── CTA final ── -->
  <CtaFinal />

</BaseLayout>
